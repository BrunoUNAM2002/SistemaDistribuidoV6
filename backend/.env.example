# Configuración de ejemplo para el frontend Flask
# Copiar este archivo a .env y ajustar valores

# ID del nodo (1, 2, 3, 4)
NODE_ID=1

# Clave secreta para sessions (cambiar en producción)
SECRET_KEY=dev-secret-key-change-in-production

# Ruta a la base de datos SQLite
# DATABASE_URI=sqlite:///../emergency_sala1.db

# Nivel de logging (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
